# Sasho マニュアル運用システム 設計書 v3

最終更新: 2026-07-21
v2からの変更: ②③の成果物をGoogleドキュメントからPDF（HTML組版→Chromeヘッドレス印刷）に変更。
スタッフへはPDFのみ公開、中間のGoogleドキュメントは廃止。

---

## 1. 目的とコンセプト

Sashoのスタッフ向けマニュアルを、単一のマスターデータベース（①DB）から
閲覧用ドキュメント（②③）を生成する構成で運用する。

- **単一の正（Single Source of Truth）**: ①DBだけが正。②③は毎回①から全文再生成する派生物。②③への直接の手直しは次回更新で消える。
- **品質ゲート**: 生煮えのデータが本番マニュアルに漏れないよう「清書完了」フラグで制御する。
- **3人運用**: 経営陣3人が同じ操作で更新できる。

---

## 2. 全体構成

```
【外部】ToDo管理表（1ファイル・2シート）
   ├ シート「現場監視_気づきリスト」
   └ シート「Todo」
   │  各シートの「マニュアル化」列（TRUE/FALSE）がフラグ
   │
   ▼  /取り込み （プル型・ボタン起動）
┌─────────────────────────────────┐
│ ①マニュアルDB（Googleスプレッドシート）        │
│   ・素起こし転記（清書完了=FALSE で着地）        │
│   ・人が清書 → 清書完了=TRUE                    │
└─────────────────────────────────┘
   │  清書完了=TRUE かつ ステータス=有効 の行のみ
   ├──────────────┬──────────────┐
   ▼              ▼              ▼
 /バッティング確認   /マニュアル更新（②）   /マニュアル更新（③）
                  Sasho｜マニュアル｜完全版.pdf   Sasho｜マニュアル｜新人向け.pdf
                  （HTML組版→PDF化）    （新人向けフラグ=TRUEのみ）
                        │
                        ├ 共有用フォルダの固定PDFを差し替え（スタッフ閲覧用）
                        └ 差し替え前に旧版を履歴フォルダへ退避
```

---

## 3. 実行環境

- **実行ツール**: Claude Code（3人全員が各自のPCで実行できる環境を整える）
- **接続方式**: Google Sheets API + Google Drive API を、**サービスアカウント方式**で直結
  - Google Cloudでサービスアカウントを1つ作成し、鍵（JSON）を3人のClaude Code環境に配置
  - ①DB・共有用フォルダ・履歴フォルダ・ToDo管理表を、サービスアカウントのメールアドレスに共有
  - 3人が同一権限で読み書きできる。個別OAuth不要
- **PDF生成**: HTMLで組版し、Chrome/Chromiumのヘッドレス印刷（--print-to-pdf）でPDF化
  - Mac/Windows両対応・追加インストール不要。Docs APIは使わない
- **編集者の記録**: DBの「最終更新者」列に、コマンド実行時に自分の名前を入れる運用で補う

---

## 4. ①マニュアルDB スキーマ

### メインシート「マニュアルDB」

| 列 | 役割 | 補足 |
|---|---|---|
| ID | 単純連番 | 最下部追加・不変。並び順に意味を持たせない |
| 大ジャンル | 粗い分類 | プルダウン（12種）。細かい章立ては②③生成時にClaudeが判断 |
| タイトル | 一行要約 | |
| 内容種別 | テキスト / チェックリスト | |
| 内容 | 本文（清書後の読める日本語） | チェックリストの場合はサマリのみ |
| チェックリストシート | 参照先シート名 | 種別=チェックリストのときのみ |
| 画像 | 画像参照 | 当面は `=IMAGE(url)` 方式（→9章） |
| 転記元 | どこから来たか | 例:「気づきリスト 2026-07-22取込」「連絡ノート移行」 |
| 清書完了 | TRUE / FALSE | **品質ゲート**。②③反映・バッティング確認の対象条件 |
| 新人向けフラグ | TRUE / FALSE | ③の対象条件 |
| ステータス | 有効 / 廃止 | |
| 起票者 / 起票日 | | |
| 最終更新者 / 最終更新日 | | |
| 備考 | 任意 | 背景・根拠・原本照合メモなど |

> **現状との差分**: 現行xlsxには「清書完了」「転記元」列が未追加。初回セットアップで追加し、
> 既存86行は 清書完了=TRUE / 転記元=「連絡ノート移行」 を一括セットする（→11章）。

### 大ジャンル（12種）

開店・閉店作業 / メニュー・料金 / ドリンク / 客席・レイアウト /
オペレーション・接客 / 清掃・衛生 / 備品・在庫 / 発注・仕入れ /
設備・機器 / 安全管理 / 予約・レジ / 調理・仕込み

### 別シート（チェックリスト専用・1リスト1シート）

増減しやすいチェックリストのみ別シート化。現状4つ:
CL_朝の準備 / CL_夕方の準備 / CL_締め作業 / CL_場所別在庫

各シート冒頭にタイトル行を置き、単体でも何のリストか分かるようにする。

### 行の粒度（「中間」方針）

- 寄せ集めの見出しは独立ルール1行ずつに分割する
- 一連の手順・分量表・配置図はまとまりとして1行に保つ
- 目的: バッティング確認が効きやすく、かつ一覧性も保つ

---

## 5. 取り込み元（ToDo管理表）の仕様 【確定】

対象ファイル: **【社内用】ToDo管理表**（Googleスプレッドシート・1ファイル）
https://docs.google.com/spreadsheets/d/1wlEMVdorrpAKG895UWYKi3JcHWi1GAxj0MERW96Nc6k

### シート1「現場監視_気づきリスト」

| ToDo表の列 | ①DBへの反映 |
|---|---|
| 内容 ＋ 議論結果 | 「内容」へ素起こし転記（議論結果に確定ルール本文が入るため両方） |
| 起票者 / 起票日 | そのまま転記 |
| マニュアル化（TRUE/FALSE） | 取り込み対象の判定条件 |

### シート2「Todo」

| ToDo表の列 | ①DBへの反映 |
|---|---|
| 内容 ＋ メモ | 「内容」へ（メモに実質の本文が入る行が多い） |
| ToDo担当者 | 「起票者」に代用 |
| マニュアル化（TRUE/FALSE） | 取り込み対象の判定条件 |

### 共通仕様

- 大ジャンルは取り込み時にClaudeが12種から仮判定 → 清書時に人が確定
- 清書完了=FALSE、ステータス=有効 で着地

### 二重取り込み防止（B案・ToDo表側マーク）

- 両シートの右端に **「取り込み済」列** を追加（初回実行時にClaude Codeが自動追加）
- `/取り込み` の対象 = **マニュアル化=TRUE かつ 取り込み済が空欄** の行
- 転記後、その行の「取り込み済」に実行日を書き込む（batchUpdate 1コール）
- マークが行自体に付くため、行挿入・並び替えでも壊れない。No.には依存しない

※2026-07-21時点のフラグ立ち行数: 気づきリスト39件 ＋ Todoシート9件 ＝ 初回取り込み48件見込み

---

## 6. データフロー（3段階ゲート）

1. **取り込み** `/取り込み` — 5章の仕様で①DB最下部へ素起こし転記
2. **清書**（人手） — 経営陣が「内容」を読める日本語に整え、清書完了=TRUE にする
3. **反映** — 清書完了=TRUE かつ 有効 の行のみが②③生成・バッティング確認の対象

---

## 7. ②③PDF出力仕様 【確定・v3改訂】

### 成果物と配置

| 成果物 | ファイル名 | 置き場所 |
|---|---|---|
| ② | Sasho｜マニュアル｜完全版.pdf | 共有用フォルダ（スタッフに閲覧共有） |
| ③ | Sasho｜マニュアル｜新人向け.pdf | 同上 |
| 旧版 | 上記ファイル名＋`_YYYY-MM-DD` | 履歴フォルダ（経営陣のみ） |

- Googleドキュメントは作らない。HTMLで組版し、Chromeヘッドレス印刷でPDF化
- HTML組版のため、表・配置図・フローチャート（HTML/CSS/SVG）・画像を自由に配置できる
- ②③の修正は必ずDB修正→再生成で行う（Coworkで指示すればよい）

### 生成方式

- 共有用フォルダの②③PDFは **ファイルIDを固定**し、`files.update` で中身だけ差し替える
  （削除→新規アップロード禁止。スタッフに配布済みのリンクを不変に保つため）
- `/マニュアル更新` の処理順:
  1. ①からHTML生成 → PDF化（文字化け確認まで）
  2. 現行PDFをDrive APIでコピーし、履歴フォルダ **「マニュアル履歴」** へ退避
  3. 退避成功を確認後、固定IDのPDFを差し替え

### 共通構成

- 表紙（タイトル・生成日）＋注意書き＋目次（ページ番号付き）
- 注意書き: 「このPDFは○月○日にマニュアルDBから自動生成されました。修正・追加の要望は経営陣へ。」
- A4縦・印刷前提。落ち着いた配色（墨色基調＋アクセント1色程度）
- 章立ては **12ジャンル固定順**（プルダウン順のまま）。章の新設・統合はClaudeに任せない
- ジャンル内の項目の並び順のみClaudeが読みやすさで判断
- 該当行が0件のジャンルは章ごと省略

### ②完全版

- 対象: 清書完了=TRUE かつ 有効 の全行
- 各項目に **ID併記**（例: `[12] ディナー ドリンク分量`）— バッティング確認結果からDBへ即座に戻れる
- チェックリストはサマリ＋シート名参照のみ（展開しない）
- 文体: 簡潔な常体ベース。備考列の背景情報も含める

### ③新人向け

- 対象: 上記のうち 新人向けフラグ=TRUE のみ
- ID・備考は非表示。純粋な読み物として
- チェックリスト（朝の準備20項目など）は **本文中に全項目展開**（新人はDBを見に行かない前提）
- 文体: です・ます調。「なぜそうするか」を一言添える

---

## 8. バッティング確認の判定基準 【確定】

- 同一の大ジャンル＋類似テーマの行で、**数値・手順・呼称が食い違う** → 「矛盾候補」としてID付きで報告
- 表現は違うが同じルールの重複 → 「重複候補」として別途報告
- 厳密な自動判定ではなく、**Claudeが文脈で候補出し → 人が最終判断** の運用

---

## 9. 画像の扱い 【暫定確定】

- Sheets API（REST）にはセル内画像を書き込むエンドポイントがなく、真のセル内画像（CellImage）は
  Apps Script経由でしか作成できない
- サービスアカウント＋REST APIで完結させる方針を優先し、**当面は `=IMAGE(url)` 関数方式** を採用
  - 画像はDriveに格納しリンク共有、URLを`=IMAGE()`で参照。values.updateのみで実現可能
- 埋め込み対象の画像が実際に届いた時点で見た目を確認し、必要ならApps Script方式を再検討
- ②③PDF側では画像・作図の制約なし（HTML組版のため、サイズ・配置・SVG作図が自由）。
  この制約はあくまで①DB（スプレッドシートのセル）側の話

---

## 10. 既存マニュアル資産の統合 【確定・A案】

DB外に存在する既存マニュアル類は、初回セットアップ時に①DBへ行として取り込み（粒度方針に従い分割）、
元ドキュメントは廃止扱いとする。以後は②③のみを見る運用に一本化。

対象（判明分）:
- インカム活用マニュアル（Googleドキュメント）
- 良介作の接客マニュアル（ドリンクお代わりルール・客室見回りルール等）
- 服装規定（素案）

※取り込み時に他の既存ドキュメントが見つかった場合も同様に統合する。

---

## 11. コマンド（Claude Code スラッシュコマンド）

| コマンド | 機能 | 対象行 |
|---|---|---|
| `/取り込み` | ToDo管理表2シートからマニュアル化=TRUE・未取り込み行を①DBへ転記し、取り込み済マークを書き込む | 5章参照 |
| `/清書チェック` | 未清書行を読める日本語に整える下書きを提案 | 清書完了=FALSE |
| `/バッティング確認` | 矛盾・重複候補をID付きで検出（8章の基準） | 清書完了=TRUE かつ 有効 |
| `/マニュアル更新` | ①から②③PDFを生成し、旧版を履歴退避後に共有用フォルダの固定PDFを差し替え（7章の仕様） | 清書完了=TRUE かつ 有効（③は新人向け=TRUEのみ） |

コマンドの中身（プロンプト・処理手順）はプロジェクトの `CLAUDE.md` およびコマンド定義ファイルに記述し、
3人が同一挙動で実行できるようにする。

---

## 12. 実装フェーズ タスクリスト

### セットアップ（1回きり）

- [ ] サービスアカウント作成・鍵JSON発行・3人の環境へ配置
- [ ] ①DB・ToDo管理表・共有用フォルダ（新規作成）・履歴フォルダ（新規作成）をサービスアカウントへ共有
- [ ] 共有用フォルダをスタッフに閲覧者として共有
- [ ] ①DBに「清書完了」「転記元」列を追加、既存86行へ 清書完了=TRUE / 転記元=「連絡ノート移行」を一括セット
- [ ] ToDo管理表2シートの右端に「取り込み済」列を追加
- [ ] CLAUDE.md＋4コマンド定義ファイルの作成・配布
- [ ] 既存マニュアル3点（10章）の①DBへの取り込み・分割

### 初回運用

- [ ] `/取り込み` 初回実行（48件見込み）→ 清書 → `/バッティング確認` → `/マニュアル更新` で②③PDF初版生成
- [ ] 初版生成時の②③PDFファイルIDをCLAUDE.mdに記入

---

## 13. 解決済みの旧・未確定事項（v1からの変更記録）

| v1の未確定事項 | 決定内容 |
|---|---|
| ToDo課題管理表の実際の列構成 | 5章の通り確定（1ファイル2シート、マニュアル化列＋取り込み済列） |
| ②③の文体・トーン | 7章の通り確定 |
| バッティング判定基準 | 8章の通り確定 |
| 画像のセル埋め込み手順 | 9章の通り暫定確定（=IMAGE方式） |
| （追加論点）既存マニュアル資産 | 10章の通りA案で統合 |
| （追加論点）②③の履歴管理 | 7章の通り履歴フォルダ方式 |
| （v3変更）②③の成果物形式 | Googleドキュメント → PDF（HTML組版・Chromeヘッドレス印刷）。Doc非公開の要望と作図・画像の自由度を両立 |
| （v3変更）ファイル名 | 「Sasho｜マニュアル｜完全版」「Sasho｜マニュアル｜新人向け」 |
