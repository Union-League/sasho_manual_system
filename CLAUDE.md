# Sasho マニュアル運用システム

このリポジトリは、Sashoのスタッフマニュアルを単一のマスターDB（①）から
閲覧用ドキュメント（②経営陣向け・③新人向け）へ生成・運用するためのものです。
詳細設計は `Sasho_manual_system_design_v3.md` を参照。

## 大原則

1. **①DBだけが正（Single Source of Truth）**。②③は毎回①から全文再生成するPDF。
2. **清書完了=TRUE かつ ステータス=有効** の行だけが②③・バッティング確認の対象。
3. ②③の修正は必ずDBを直してから再生成する（PDFは直接編集できない・しない）。
4. コマンド実行前に必ず実行者の名前を確認し、DBを書き換えた場合は「最終更新者」「最終更新日」を記録する。

## 図表化の原則（②③PDF・HTML版の両方に適用）

**単純な転記は禁止。**表・配置図・対比で示すべき内容は必ずその形にする。
判断は毎回やり直さない。`/清書チェック` で決めて保存し、
以降は全ての成果物がそれを読んで同じ図を描く。

置き場所は**規模で2通りに分ける**（簡単な表のためにシートを増やさない）。

| | 置き場所 | 対象 |
|---|---|---|
| 簡単な表・手順 | **「内容」列にそのまま書く** | 3列以下かつ4行程度まで。番号付き手順 |
| 複雑な図表 | **1ID1シート「ID○○」** | 配置図、4列以上の表、行数の多い分量表、対象別の対比 |

### 「内容」列の書き方（簡単な表・手順）

- **表**: `｜`（全角の縦棒）区切りの行を2行以上続けると表になる。**1行目がヘッダー行**。
  表の前後は空行で区切る（段落単位で判定するため）。
- **手順**: `①②③…` で始まる行だけを並べると番号付きリストになる。
- どちらも成果物では表・リストとして組まれる。「内容」列を読んでも意味が通る形なので、
  DB上でも人が直せる。

```
区分｜キャンセル料
ランチ｜¥3,000 / 人
ディナー｜¥5,000 / 人
```

### 別シート「ID○○」の作り方（複雑な図表）

- シート名は **`ID` ＋ 数字だけ**（例 `ID28`）。タブ幅を詰めるため他は書かない。
- レイアウト: **A1=`【タイトル】`／B1=表示形式／2行目=列見出し／3行目以降=データ**
  1行目はマージしない（マージすると B1 への書き込みが消える）。
- 表示形式: 表 / 配置図 / 対比 / 色見本 / タイムライン / 手順 / 対応表 / チェックリスト
  （表・対応表は2行目がそのままヘッダー行になる。他は列の意味が位置で決まる）
- チェックリストも同じ構造の別シートとして持つ（A列=No / B列=項目内容）。
  項目が随時増減するため、シート側で書き足せることを優先する。
- **①DBの「別シート」列にシート名を書く**。書かないと別シートの存在が分からない。
- 「内容」列は要約1行にする（同じ情報を2箇所に持たない＝大原則1）。
  ただし図表に入らない補足（例外・注意・理由）は要約側に残す。
- 図表の中身も検索対象に含める（要約だけでは図表内の語で引けない）。

## 成果物（②③）

- ②完全版: `Sasho｜マニュアル｜完全版.pdf`
- ③新人向け: `Sasho｜マニュアル｜新人向け.pdf`
- HTMLで組版し、Chromeヘッドレスの --print-to-pdf でPDF化。Googleドキュメントは使わない。
- 共有用フォルダ内の固定PDF（IDは下表）を files.update で中身だけ差し替える。
  スタッフに配布済みのリンクを不変に保つため、削除→新規アップロードは禁止。
- 差し替え前に旧版を履歴フォルダへコピー退避する。

## 接続情報

- 認証: サービスアカウント方式。鍵JSONは `~/.sasho/service-account.json` に配置。
- 使用API: Google Sheets API / Google Drive API
- Pythonの `google-api-python-client` + `google-auth` を使用する。
  未インストールなら `pip install google-api-python-client google-auth` を実行してよい。

### ファイルID（セットアップ時に記入）

| 名前 | ID | 備考 |
|---|---|---|
| ①マニュアルDB | `1VxEepC6PHtTO_Ic1JckaKMP9lItFCqhLLc5cpE2Gkkk` | メインシート名: マニュアルDB |
| ToDo管理表 | `1wlEMVdorrpAKG895UWYKi3JcHWi1GAxj0MERW96Nc6k` | シート: 現場監視_気づきリスト / Todo |
| 共有用フォルダ | `14c9fq29nca5tuMIxpO-szOHTxpBlFpfK` | スタッフ閲覧用。②③のPDFを置く |
| ②完全版PDF | `1m5bnWYeIpY_zigdAxQzfgdwotoXk4rvi` | 2026-07-27 初版作成。以後 files.update で中身のみ差し替え |
| ③新人向けPDF | `<<PDF3_ID>>` | 同上 |
| 履歴フォルダ「マニュアル履歴」 | `1Uoiu_cEJ82ADlJ4Se7Np_y6a64zB88jD` | 旧版PDFの退避先。経営陣のみ閲覧 |

## ①DBのスキーマ（列順）

ID / 大ジャンル / タイトル / 内容種別 / 内容 / 別シート / 画像 /
転記元 / 清書完了 / 新人向けフラグ / ステータス / 起票者 / 起票日 /
最終更新者 / 最終更新日 / 備考 / 検索キーワード

- 備考・検索キーワードは**内部データ。②③PDFには出力しない**。
- 検索キーワードはHTML版マニュアルの検索用。/マニュアル更新 が空欄の行に自動生成する。

- ID: 単純連番。行は必ず最下部に追加。並び順に意味を持たせない。
- 別シート（F列）: 中身を別シートに持つ行だけシート名を書く。**空欄なら別シートは無い**。
  シート名は **`ID` ＋ その行のID**（例 `ID28`）。図表もチェックリストも同じ命名で、
  **どちらなのかはシートのB1（表示形式）で決まる**。1行1シートの対応。
  シートは決め打ちせずファイル内から動的に列挙する（増減するため）。
- 内容種別: テキスト / チェックリスト。チェックリストは「別シート」列で `ID○○` を参照し、
  そのシートのB1に `チェックリスト` と書く。
- 大ジャンル（13種・この順を②③の章立て順にも使う）:
  開店・閉店作業 / メニュー・料金 / ドリンク / 客席・レイアウト /
  オペレーション・接客 / 清掃・衛生 / 備品・在庫 / 発注・仕入れ /
  設備・機器 / 安全管理 / 予約・レジ / 調理・仕込み / 身だしなみ

## 行の粒度（「中間」方針）

- 寄せ集めの見出しは独立ルール1行ずつに分割する。
- 一連の手順・分量表・配置図はまとまりとして1行に保つ。

## コマンド

`.claude/commands/` に定義。/取り込み → 清書（人手＋/清書チェック補助）→
/バッティング確認 → /マニュアル更新 の流れで運用する。

## 禁止事項

- ①DBの既存行の削除（廃止はステータス=廃止で表現する）
- IDの振り直し・行の並び替え
- 共有用フォルダのPDFの削除・新規アップロードによる差し替え（files.updateのみ許可）
- ToDo管理表の「取り込み済」列以外への書き込み

